import { MaterialIcons } from "@expo/vector-icons";

// Lista de nomes válidos de ícones do MaterialIcons extraída do glyphMap
export const materialIconNames: string[] = Object.keys(MaterialIcons.glyphMap);